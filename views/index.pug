extends layout

block content

	.posts
		for post in posts
			p.post #{post.body}

	form(action='/addpost' method='POST')
		label(for='postBody') Add Post
		textarea(name='postBody')
		input(type='submit' value='Add Post')

